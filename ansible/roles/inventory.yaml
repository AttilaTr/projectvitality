all:
  children:
    managers:
      hosts:
        swarm-manager:
    workers:
      hosts:
        swarm-worker:
        swarm-worker2:
    nginx:
      hosts:
        10.154.0.16: 

  vars:
    ansible_python_interpreter: "/usr/bin/python3"